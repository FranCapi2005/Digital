let heroes = require('./superheroes/index');
console.log(heroes)

let fs = require('fs');
console.log(fs)

// writeFile / writeFileSync    (nos permiten escribir archivos)
// readFile / readFileSync      (nos permiten leer archivos)
// open                         (nos permite abrir un archivo)
// mkdir                        (nos permite crear un directorio)
// lchmod / lchmodSync          (nos permiten cambiar permisos)