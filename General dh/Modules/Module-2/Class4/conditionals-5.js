let velocidad = 268;
let altura = 250;

if ((velocidad >= 268 && velocidad <= 278) && (altura >= 150 && altura <= 300)) { // se tienen que cumplir ambas condiciones para poder ejecutar el resultado
    console.log("Puede Aterrizar");
} else {
    console.log ("No puede aterrizar")
}