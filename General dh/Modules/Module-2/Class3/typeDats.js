/*Comentarios para dar informacion de lo que se esta haciendo */

// Dato tipo number
let edad = 20;
console.log(edad); 

// Dato tipo number con decimales
let precio = 150.48;
console.log(precio);

// Dato tipo string
let nombre = "Francisco";
console.log(nombre);

// Dato tipo string
let apellido = 'Capitani'
console.log(apellido);

// Dato tipo booleano con valor True
let verdadero = true;
console.log(verdadero);

// Dato tipo booleano con valor False
let falso = false;
console.log(falso);

// Dato tipo NaN (ecuacion o resultado imposible, ej: "Pato" * 7)
let notANumber = NaN;
console.log(notANumber);

// Dato tipo null
let valorNulo = null;
console.log(valorNulo);

// Dato tipo undefined
let sinValor;
console.log(sinValor);

// Dato tipo Objeto Literal
let miComputadora = {
    marca: 'HP',
    modelo: 2019,
    estaEncendida: true
};
console.log(miComputadora);

// Dato tipo Array
let seriesFavoritas = [ 'Manifest', 'Flash', 'Spiderman'];
// Indice                   0          1          2
console.log(seriesFavoritas);
console.table(seriesFavoritas); /*Crea una tabla de los valores a imprimir*/


