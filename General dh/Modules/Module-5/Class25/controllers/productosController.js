const controlador = { /* Metodos, funciones que se van a encargar de manejar a los productos */
    index: (req, res) => {
        res.send("Index de productos");
    }
};

module.exports = controlador;